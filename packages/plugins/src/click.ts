import { EventBusReturn } from "@bottle-monitor/types"
import { InitOptions } from "@bottle-monitor/types"

export const ClickPlugin = (eventBus: EventBusReturn, initOptions: InitOptions, ids: string[], classes: string[], xpath: string[]) => {
    
}